<!DOCTYPE html>

<html>

<head><title>센서 데이터</title></head>
<body>
    <h1>센서 기록</h1>
    <p>페이지 로딩 시각: {{ now }}</p>
    <table border="1">
        <tr>
            <th>번호</th>
            <th>온도 (°C)</th>
            <th>습도 (%)</th>
        </tr>
        {% if not data %}
            <p>데이터 없음</p>
        {% else %}
        {% for row in data %}
        <tr>
            <td>{{ loop.index }}</td>
            <td>{{ row.temperature }}</td>
            <td>{{ row.humidity }}</td>
            <td>
                {% if row.temperature >= 28 %}
                    🔴 더움
                {% elif row.temperature >= 24 %}
                    🟡 보통
                {% else %}
                    🟢 쾌적
                {% endif %}
            </td>
        </tr>
        {% endfor %}
        {% endif %}
    </table>
</body>

</html>
